# Vault configuration
VAULT_ROOT=/Users/enzo/OBS_Lavoro

# Server WebSocket URL (use wss:// for production)
SERVER_URL=wss://snorkel.vaplayground.cloud/ws

# API Key (must match server API_KEY)
# Generate with: openssl rand -hex 32
API_KEY=

# Node environment
NODE_ENV=production
